{# @var plugin \wsydney76\elementmap\ElementmapPlugin #}
{# @var settings \wsydney76\elementmap\models\Settings #}

{% import '_includes/forms.twig' as forms %}

{{ forms.selectizeField({
    label: 'Look for relationships on which site?'|t('_elementmap'),
    name: 'showSites',
    value: settings.showSites,
    options: [
        {value: 'all', label: 'On all sites'|t('_elementmap')},
        {value: 'current', label: 'On active site only'|t('_elementmap')},
    ],
    warning: config.showSites is defined ? _self.configWarning('showSites'),
}) }}

{{ forms.selectizeField({
    label: 'Show image thumbnails?'|t('_elementmap'),
    name: 'showThumbnails',
    value: settings.showThumbnails,
    options: [
        {value: 'true', label: 'Show small thumbnails for link to image assets'|t('_elementmap')},
        {value: 'false', label: 'Show icon'|t('_elementmap')},
    ],
    tip: 'This slows down performance significantly, so disable it if a lot of images are related to an element.'|t('_elementmap'),
    warning: config.showThumbnails is defined ? _self.configWarning('showThumbnails'),
}) }}

{{ forms.selectizeField({
    label: 'Where the link for a found relationship to a nested element should point to'|t('_elementmap'),
    name: 'linkToElement',
    value: settings.linkToElement,
    options: [
        {value: 'root', label: 'Link to top level element'|t('_elementmap')},
        {value: 'nested', label: 'Link to nested element'|t('_elementmap')},
    ],
    warning: config.linkToElement is defined ? _self.configWarning('linkToElement')
}) }}

{{ forms.selectizeField({
    label: 'Show root owners?'|t('_elementmap'),
    name: 'showRootOwner',
    value: settings.showRootOwner,
    options: [
        {value: 'true', label: 'Show root owners for nested elements'|t('_elementmap')},
        {value: 'false', label: 'Show nested element only'|t('_elementmap')},
    ],
    tip: 'Showing root owners improves user experience significantly, but adds a lot of overhead.'|t('_elementmap'),
    warning: config.showRootOwner is defined ? _self.configWarning('showRootOwner'),
}) }}

{{ forms.selectizeField({
    label: 'Show revisions?'|t('_elementmap'),
    name: 'showRevisions',
    value: settings.showRevisions,
    options: [
        {value: 'false', label: 'Show canonical elements and drafts'|t('_elementmap')},
        {value: 'true', label: 'Also show revisions'|t('_elementmap')},
    ],
    warning: config.showRevisions is defined ? _self.configWarning('showRevisions'),
}) }}

{{ forms.selectizeField({
    label: 'Sort results by'|t('_elementmap'),
    name: 'orderResults',
    value: settings.orderResults,
    options: [
        {value: 'elementType', label: 'Element type and title'|t('_elementmap')},
        {value: 'title', label: 'Title'|t('_elementmap')},
    ],
    warning: config.orderResults is defined ? _self.configWarning('orderResults'),
}) }}
